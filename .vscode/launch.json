{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "buildFlags": "-buildvcs=false",
            "program": "${workspaceFolder}"
        }
    ]
}